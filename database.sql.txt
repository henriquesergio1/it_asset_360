-- IT Asset 360 - Script Completo de Banco de Dados (SQL Server)
-- Versão: 3.22.1
-- Este script contém todas as tabelas necessárias para o funcionamento inicial do sistema atualizado.

-- Tabela de Dispositivos (Devices)
CREATE TABLE Devices (
    Id NVARCHAR(255) PRIMARY KEY,
    AssetTag NVARCHAR(255) UNIQUE,
    Status NVARCHAR(50),
    PreviousStatus NVARCHAR(50) NULL,
    ModelId NVARCHAR(255),
    SerialNumber NVARCHAR(255),
    InternalCode NVARCHAR(255),
    Imei NVARCHAR(255) UNIQUE,
    PulsusId NVARCHAR(255),
    CurrentUserId NVARCHAR(255),
    SectorId NVARCHAR(255),
    CostCenter NVARCHAR(255),
    LinkedSimId NVARCHAR(255),
    PurchaseDate DATETIME,
    PurchaseCost FLOAT,
    InvoiceNumber NVARCHAR(255),
    Supplier NVARCHAR(255),
    PurchaseInvoiceBinary VARBINARY(MAX),
    DeviceImageBinary VARBINARY(MAX),
    CustomData NVARCHAR(MAX)
);

-- Tabela de Chips (SIM Cards)
CREATE TABLE SimCards (
    Id NVARCHAR(255) PRIMARY KEY,
    PhoneNumber NVARCHAR(50) UNIQUE,
    Operator NVARCHAR(100),
    Iccid NVARCHAR(255) UNIQUE,
    Status NVARCHAR(50),
    CurrentUserId NVARCHAR(255),
    PlanDetails NVARCHAR(MAX)
);

-- Tabela de Colaboradores (Users)
CREATE TABLE Users (
    Id NVARCHAR(255) PRIMARY KEY,
    FullName NVARCHAR(255),
    Cpf NVARCHAR(50) UNIQUE,
    Rg NVARCHAR(50),
    Pis NVARCHAR(50),
    Address NVARCHAR(MAX),
    Email NVARCHAR(255) UNIQUE,
    SectorId NVARCHAR(255),
    InternalCode NVARCHAR(255),
    Active BIT,
    Status NVARCHAR(50) DEFAULT 'Ativo',
    OnLeaveUntil DATETIME NULL,
    HasPendingIssues BIT,
    PendingIssuesNote NVARCHAR(MAX)
);

-- Tabela de Logs de Auditoria (AuditLogs)
CREATE TABLE AuditLogs (
    Id NVARCHAR(255) PRIMARY KEY,
    AssetId NVARCHAR(255),
    AssetType NVARCHAR(100),
    Action NVARCHAR(100),
    Timestamp DATETIME DEFAULT GETDATE(),
    AdminUser NVARCHAR(255),
    TargetName NVARCHAR(255),
    Notes NVARCHAR(MAX),
    BackupData NVARCHAR(MAX),
    PreviousData NVARCHAR(MAX),
    NewData NVARCHAR(MAX)
);

-- Tabela de Usuários do Sistema (SystemUsers)
CREATE TABLE SystemUsers (
    Id NVARCHAR(255) PRIMARY KEY, 
    Name NVARCHAR(255), 
    Email NVARCHAR(255) UNIQUE, 
    Password NVARCHAR(255), 
    Role NVARCHAR(50)
);

-- Tabela de Configurações do Sistema (SystemSettings)
CREATE TABLE SystemSettings (
    Id INT PRIMARY KEY IDENTITY(1,1), 
    AppName NVARCHAR(255), 
    LogoUrl NVARCHAR(MAX), 
    Cnpj NVARCHAR(50), 
    TermTemplate NVARCHAR(MAX), 
    AccentColor NVARCHAR(50) DEFAULT '#2563eb', 
    LicenseKey NVARCHAR(MAX), 
    LicenseClient NVARCHAR(255), 
    LicenseExpires DATETIME
);

-- Tabela de Modelos (Models)
CREATE TABLE Models (
    Id NVARCHAR(255) PRIMARY KEY, 
    Name NVARCHAR(255), 
    BrandId NVARCHAR(255), 
    TypeId NVARCHAR(255), 
    ImageBinary VARBINARY(MAX)
);

-- Tabela de Marcas (Brands)
CREATE TABLE Brands (
    Id NVARCHAR(255) PRIMARY KEY, 
    Name NVARCHAR(255) UNIQUE
);

-- Tabela de Tipos de Ativos (AssetTypes)
CREATE TABLE AssetTypes (
    Id NVARCHAR(255) PRIMARY KEY, 
    Name NVARCHAR(255) UNIQUE, 
    CustomFieldIds NVARCHAR(MAX),
    AllowMultipleUsers BIT DEFAULT 0
);

-- Tabela de Manutenção (MaintenanceRecords)
CREATE TABLE MaintenanceRecords (
    Id NVARCHAR(255) PRIMARY KEY, 
    DeviceId NVARCHAR(255), 
    Description NVARCHAR(MAX), 
    Cost FLOAT, 
    Date DATETIME, 
    Type NVARCHAR(100), 
    Provider NVARCHAR(255), 
    InvoiceBinary VARBINARY(MAX)
);

-- Tabela de Setores (Sectors)
CREATE TABLE Sectors (
    Id NVARCHAR(255) PRIMARY KEY, 
    Name NVARCHAR(255) UNIQUE
);

-- Tabela de Termos (Terms)
CREATE TABLE Terms (
    Id NVARCHAR(255) PRIMARY KEY, 
    UserId NVARCHAR(255), 
    Type NVARCHAR(50), 
    AssetDetails NVARCHAR(MAX), 
    Date DATETIME, 
    FileBinary VARBINARY(MAX), 
    IsManual BIT DEFAULT 0, 
    ResolutionReason NVARCHAR(MAX),
    EvidenceBinary VARBINARY(MAX) NULL,
    Condition NVARCHAR(50) NULL,
    DamageDescription NVARCHAR(MAX) NULL,
    Evidence2Binary VARBINARY(MAX) NULL,
    Evidence3Binary VARBINARY(MAX) NULL,
    Notes NVARCHAR(MAX) NULL,
    SignatureToken NVARCHAR(255) NULL,
    SignatureDate DATETIME NULL,
    SignatureIp NVARCHAR(50) NULL,
    SignatureLocation NVARCHAR(MAX) NULL,
    SignatureCanvasBinary VARBINARY(MAX) NULL,
    SignatureDocumentPhoto VARBINARY(MAX) NULL,
    SignatureHash NVARCHAR(255) NULL
);

-- Tabela de Tipos de Acessórios (AccessoryTypes)
CREATE TABLE AccessoryTypes (
    Id NVARCHAR(255) PRIMARY KEY, 
    Name NVARCHAR(255) UNIQUE
);

-- Tabela de Acessórios (DeviceAccessories)
CREATE TABLE DeviceAccessories (
    Id NVARCHAR(255) PRIMARY KEY, 
    DeviceId NVARCHAR(255), 
    AccessoryTypeId NVARCHAR(255), 
    Name NVARCHAR(255)
);

-- Tabela de Campos Personalizados (CustomFields)
CREATE TABLE CustomFields (
    Id NVARCHAR(255) PRIMARY KEY, 
    Name NVARCHAR(255) UNIQUE
);

-- Tabela de Contas e Softwares (SoftwareAccounts)
CREATE TABLE SoftwareAccounts (
    Id NVARCHAR(255) PRIMARY KEY, 
    Name NVARCHAR(255), 
    Type NVARCHAR(100), 
    Login NVARCHAR(255), 
    Password NVARCHAR(255), 
    AccessUrl NVARCHAR(MAX), 
    Status NVARCHAR(50), 
    UserIds NVARCHAR(MAX), 
    DeviceIds NVARCHAR(MAX), 
    SectorId NVARCHAR(255), 
    Notes NVARCHAR(MAX)
);

-- Tabela de Overrides de Expediente
CREATE TABLE ExpedienteOverrides (
    Id NVARCHAR(255) PRIMARY KEY,
    Codigo NVARCHAR(255) UNIQUE,
    Observation NVARCHAR(MAX),
    ReactivationDate DATETIME NULL
);

-- Gestão de Tarefas (Tasks)
CREATE TABLE Tasks (
    Id NVARCHAR(255) PRIMARY KEY,
    Title NVARCHAR(255),
    Description NVARCHAR(MAX),
    Type NVARCHAR(100),
    Status NVARCHAR(50),
    CreatedAt DATETIME DEFAULT GETDATE(),
    DueDate DATETIME,
    AssignedTo NVARCHAR(255),
    Comments NVARCHAR(MAX),
    Instructions NVARCHAR(MAX),
    EvidenceUrls NVARCHAR(MAX),
    ManualAttachments NVARCHAR(MAX),
    DeviceId NVARCHAR(255),
    MaintenanceType NVARCHAR(100),
    MaintenanceCost FLOAT,
    MaintenanceItems NVARCHAR(MAX)
);

-- Histórico de Tarefas (TaskLogs)
CREATE TABLE TaskLogs (
    Id NVARCHAR(255) PRIMARY KEY,
    TaskId NVARCHAR(255),
    Action NVARCHAR(MAX),
    AdminUser NVARCHAR(255),
    Timestamp DATETIME DEFAULT GETDATE(),
    Notes NVARCHAR(MAX)
);

-- Configuração de DB Externo
CREATE TABLE ExternalDbConfig (
    Id INT PRIMARY KEY IDENTITY(1,1),
    Technology NVARCHAR(50),
    Host NVARCHAR(255),
    Port INT,
    Username NVARCHAR(255),
    Password NVARCHAR(255),
    DatabaseName NVARCHAR(255),
    SelectionQuery NVARCHAR(MAX),
    LastSync DATETIME
);

-- Insumos (Consumables)
CREATE TABLE Consumables (
    Id NVARCHAR(255) PRIMARY KEY,
    Name NVARCHAR(255) NOT NULL,
    Category NVARCHAR(100) NOT NULL,
    CurrentStock INT DEFAULT 0,
    MinStock INT DEFAULT 0,
    Unit NVARCHAR(50) NOT NULL,
    CreatedAt DATETIME DEFAULT GETDATE()
);

-- Transações de Insumos (ConsumableTransactions)
CREATE TABLE ConsumableTransactions (
    Id NVARCHAR(255) PRIMARY KEY,
    ConsumableId NVARCHAR(255) FOREIGN KEY REFERENCES Consumables(Id) ON DELETE CASCADE,
    Type NVARCHAR(50) NOT NULL,
    Quantity INT NOT NULL,
    Date DATETIME DEFAULT GETDATE(),
    AdminUser NVARCHAR(255),
    Notes NVARCHAR(MAX)
);

-- Índices Recomendados
CREATE INDEX IX_Devices_Status ON Devices (Status);
CREATE INDEX IX_Devices_ModelId ON Devices (ModelId);
CREATE INDEX IX_Devices_CurrentUserId ON Devices (CurrentUserId);
CREATE INDEX IX_Devices_SectorId ON Devices (SectorId);
CREATE INDEX IX_Devices_LinkedSimId ON Devices (LinkedSimId);
CREATE INDEX IX_Tasks_Status ON Tasks (Status);
CREATE INDEX IX_Tasks_AssignedTo ON Tasks (AssignedTo);
CREATE INDEX IX_Tasks_DeviceId ON Tasks (DeviceId);
CREATE INDEX IX_Users_Active ON Users (Active);
CREATE INDEX IX_Users_SectorId ON Users (SectorId);
CREATE INDEX IX_SimCards_Status ON SimCards (Status);
CREATE INDEX IX_SimCards_Operator ON SimCards (Operator);

-- Dados Iniciais
IF NOT EXISTS (SELECT 1 FROM SystemSettings)
BEGIN
    INSERT INTO SystemSettings (AppName, LogoUrl, AccentColor) VALUES ('IT Asset 360', '', '#2563eb');
END

IF NOT EXISTS (SELECT 1 FROM ExternalDbConfig)
BEGIN
    INSERT INTO ExternalDbConfig (Technology) VALUES ('SQL Server');
END

-- =================================================================================
-- MÓDULO FUEL360 - TABELAS PREFIXADAS (Fuel*)
-- =================================================================================

IF OBJECT_ID('FuelGrupos', 'U') IS NULL
BEGIN
    CREATE TABLE FuelGrupos (
        ID_Grupo INT IDENTITY(1,1) PRIMARY KEY,
        Nome NVARCHAR(100) UNIQUE NOT NULL,
        DataCriacao DATETIME DEFAULT GETDATE()
    );
END

IF OBJECT_ID('FuelColaboradores', 'U') IS NULL
BEGIN
    CREATE TABLE FuelColaboradores (
        ID_Colaborador INT IDENTITY(1,1) PRIMARY KEY,
        ID_Pulsus INT UNIQUE NOT NULL, 
        CodigoSetor INT NOT NULL,
        Nome NVARCHAR(200) NOT NULL,
        Grupo NVARCHAR(100) NOT NULL, 
        TipoVeiculo NVARCHAR(50) CHECK (TipoVeiculo IN ('Carro', 'Moto', 'Sem Veículo / VT')),
        Ativo BIT DEFAULT 1,
        EnderecoBase NVARCHAR(MAX),
        LatitudeBase DECIMAL(12, 9),
        LongitudeBase DECIMAL(12, 9),
        EnderecoPendente BIT DEFAULT 0,
        DataCriacao DATETIME DEFAULT GETDATE()
    );
END

IF OBJECT_ID('FuelUsuarios', 'U') IS NULL
BEGIN
    CREATE TABLE FuelUsuarios (
        ID_Usuario INT IDENTITY(1,1) PRIMARY KEY,
        Nome NVARCHAR(200) NOT NULL,
        Usuario NVARCHAR(100) UNIQUE NOT NULL,
        SenhaHash NVARCHAR(MAX) NOT NULL,
        Perfil NVARCHAR(50) DEFAULT 'Admin',
        Ativo BIT DEFAULT 1
    );
END

IF OBJECT_ID('FuelSystemSettings', 'U') IS NULL
BEGIN
    CREATE TABLE FuelSystemSettings (
        ID INT PRIMARY KEY CHECK (ID = 1),
        CompanyName NVARCHAR(200) DEFAULT 'Fuel360 Enterprise',
        LogoUrl NVARCHAR(MAX),
        FuelPrice DECIMAL(10, 4) DEFAULT 5.89,
        KmL_Car INT DEFAULT 10,
        KmL_Moto INT DEFAULT 35,
        Alert_MaxDailyKM INT DEFAULT 400,
        Alert_MaxClientDist INT DEFAULT 100,
        LicenseKey NVARCHAR(MAX),
        LicenseClient NVARCHAR(200),
        LicenseExpires DATETIME
    );
END

IF OBJECT_ID('FuelReembolsoHistorico', 'U') IS NULL
BEGIN
    CREATE TABLE FuelReembolsoHistorico (
        ID_Historico INT IDENTITY(1,1) PRIMARY KEY,
        Periodo NVARCHAR(100) NOT NULL,
        DataFechamento DATETIME DEFAULT GETDATE(),
        TotalGeral DECIMAL(18, 2) NOT NULL,
        UsuarioFechamento NVARCHAR(100),
        OrigemDados NVARCHAR(50) DEFAULT 'CSV',
        MotivoEdicao NVARCHAR(MAX),
        ID_RotaHist INT
    );
END

IF OBJECT_ID('FuelReembolsoDetalhe', 'U') IS NULL
BEGIN
    CREATE TABLE FuelReembolsoDetalhe (
        ID_Detalhe INT IDENTITY(1,1) PRIMARY KEY,
        ID_Historico INT NOT NULL,
        ID_Pulsus INT NOT NULL,
        NomeColaborador NVARCHAR(200),
        Grupo NVARCHAR(100),
        TipoVeiculo NVARCHAR(50),
        TotalKM DECIMAL(18, 4),
        ValorReembolso DECIMAL(18, 2),
        ParametroPreco DECIMAL(10, 4),
        ParametroKmL INT,
        Efetividade DECIMAL(5, 4),
        Ajuste DECIMAL(18, 2) DEFAULT 0,
        FOREIGN KEY (ID_Historico) REFERENCES FuelReembolsoHistorico(ID_Historico) ON DELETE CASCADE
    );
END

IF OBJECT_ID('FuelReembolsoDiario', 'U') IS NULL
BEGIN
    CREATE TABLE FuelReembolsoDiario (
        ID_Diario INT IDENTITY(1,1) PRIMARY KEY,
        ID_Detalhe INT NOT NULL,
        DataOcorrencia DATE NOT NULL,
        KM_Dia DECIMAL(18, 4),
        Valor_Dia DECIMAL(18, 2),
        Observacao NVARCHAR(MAX),
        FOREIGN KEY (ID_Detalhe) REFERENCES FuelReembolsoDetalhe(ID_Detalhe) ON DELETE CASCADE
    );
END

IF OBJECT_ID('FuelAusencias', 'U') IS NULL
BEGIN
    CREATE TABLE FuelAusencias (
        ID_Ausencia INT IDENTITY(1,1) PRIMARY KEY,
        ID_Colaborador INT NOT NULL,
        DataInicio DATE NOT NULL,
        DataFim DATE NOT NULL,
        Motivo NVARCHAR(200) NOT NULL,
        FOREIGN KEY (ID_Colaborador) REFERENCES FuelColaboradores(ID_Colaborador)
    );
END

IF OBJECT_ID('FuelLogsSistema', 'U') IS NULL
BEGIN
    CREATE TABLE FuelLogsSistema (
        ID_Log INT IDENTITY(1,1) PRIMARY KEY,
        DataHora DATETIME DEFAULT GETDATE(),
        Usuario NVARCHAR(100),
        Acao NVARCHAR(100),
        Detalhes NVARCHAR(MAX)
    );
END

PRINT 'Banco de dados sincronizado e atualizado com sucesso (Módulos TI, RH e Fuel360).';

